!(({languages}) => {
  function a(a, e) {
    languages[a] &&
      languages.insertBefore(a, "comment", { "doc-comment": e });
  }
  const e = languages.markup.tag;

  const t = {
    pattern: /\/\/\/.*/,
    greedy: !0,
    alias: "comment",
    inside: { tag: e },
  };

  const g = { pattern: /'''.*/, greedy: !0, alias: "comment", inside: { tag: e } };
  a("csharp", t), a("fsharp", t), a("vbnet", g);
})(Prism);
